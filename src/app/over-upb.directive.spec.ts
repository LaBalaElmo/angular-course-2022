import { OverUpbDirective } from './over-upb.directive';

describe('OverUpbDirective', () => {
  it('should create an instance', () => {
    const directive = new OverUpbDirective();
    expect(directive).toBeTruthy();
  });
});
